import { detectPlatform } from '@/utils/udn-newmedia-utils';

const _detectPlatform = {
  methods: {
    detectPlatform: function() {
      return detectPlatform();
    },
  }
}

export default _detectPlatform